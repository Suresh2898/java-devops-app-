apiVersion: v1
kind: Service
metadata:
  name: java-devops-app-service
spec:
  type: NodePort

  selector:
    app: java-devops-app

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080